-- 코드를 입력하세요
SELECT   NAME,
         COUNT
FROM    (
    SELECT   NAME,
             COUNT(NAME) AS COUNT
    FROM     ANIMAL_INS
    WHERE    NAME IS NOT NULL
    GROUP BY NAME) AS Z
    WHERE    COUNT >= 2
ORDER BY NAME ASC