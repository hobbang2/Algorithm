#include <cstdio>
#include <queue>
#include <vector>

using namespace std;

int main() {
	int N = 0;
	int num = 0;
	int target = 0;
	vector <int> nums;
	queue <int> hubo;s

	while (N--) {
		scanf("%d", &num);
		nums.push_back(num);
	}
	
	target = nums.back();
	hubo.push(nums[0]);

	while (hubo.empty() == false) {
		int curValue 
	}
	

	return 0;
}